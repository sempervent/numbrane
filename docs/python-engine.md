# Python engine

Home: `engines/python/` (`numbrane_python`).

- Deterministic RNG aligned with NAP vectors
- Fields, simulation, render, parameters, compose, meta, interpret (rule-based), sketches
- Sacred geometry and landscape modules
- Piece registry and CLI render path
- LATTICEFALL world builder (`pieces.latticefall`)

```bash
just test-python
just lint-python
just render flagship/latticefall seed=42
```
