# Web engine

Interactive browser runtime (Vite + TypeScript + WebGL2 + Tone.js).

```bash
just latticefall-build
just dev-web
just latticefall
just test-web
just latticefall-smoke
```

WASM bindings are generated under `src/wasm/pkg/` (gitignored); run `just latticefall-build`.
