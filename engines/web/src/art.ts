/** RETIRED — optional p5.js path (not imported). */
export function startArt(_root: HTMLElement): never {
  throw new Error(
    "art.ts is retired: use visual.ts (WebGL). p5 path is optional and not wired.",
  );
}
