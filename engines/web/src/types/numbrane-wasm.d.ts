// The generated wasm-bindgen package is intentionally absent from a clean checkout.
// Runtime builds create it with `just latticefall-build`; fast typechecks only need
// to know that the dynamic loader target is a module.
declare module "*numbrane_wasm.js";
